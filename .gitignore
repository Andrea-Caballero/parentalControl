*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
/.idea/misc.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Local Pi runtime state
.atl/

# Manual test evidence (PNGs from on-device runs)
# Kept out of the branch so reviewers don't see the user's local
# screen captures; can be restored from /tmp/opencode/ if needed.
/cross-device/

# Developer local backup
/local.properties.bak

# Supabase CLI runtime cache
/supabase/.temp/

# Archived openspec change artifacts from prior sessions
# (kept on disk for reference but not in the branch)
/openspec/changes/archive/
